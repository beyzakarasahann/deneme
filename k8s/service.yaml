apiVersion: v1
kind: Service
metadata:
  name: beyza-lab
  labels:
    app: beyza-lab
spec:
  type: ClusterIP
  selector:
    app: beyza-lab
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
