apiVersion: v1
kind: Service
metadata:
  name: beyza-lab
  namespace: beyza-lab
  labels:
    app: beyza-lab
spec:
  type: NodePort
  selector:
    app: beyza-lab
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
